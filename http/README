cli_1.cc: 长连接通信测试, 若启动keep-alive, 则在通信活跃期间，服务器不会主动断开连接
cli_2.cc: 非活跃超时释放测试，当启动了keep-alive, 
            但是在通信活跃期间，服务器主动断开连接
cli_3.cc: 给服务器发送1024字节的数据，但是实际上Content-Length并不正确，观察服务器行为
{
    1、如果数据只发送一次，服务器将得不到完整请求，客户端也得不到完整响应，直到超时释放
    2、如果数据分多次发送，服务器会将后面的请求当做前面请求的一部分，后面会因为处理错误而断开连接
}
cli_4:cc: 大文件传输测试，给服务器上传一个大文件，观察处理结果
